diff --git "a/D:\\projects\\github\\todo2api\\work\\protocol-first-registration\\ORIGINAL_FILE" "b/D:\\projects\\github\\todo2api\\work\\protocol-first-registration\\MODIFIED_FILE"
index 7a6a630..4006990 100644
--- "a/D:\\projects\\github\\todo2api\\work\\protocol-first-registration\\ORIGINAL_FILE"
+++ "b/D:\\projects\\github\\todo2api\\work\\protocol-first-registration\\MODIFIED_FILE"
@@ -673,7 +673,6 @@ def process_one_account(
     for attempt in range(1, max_retries + 1):
         account: dict | None = None
         todo: TodoforAI | None = None
-        captcha_token = ""
         try:
             last_session_error: Exception | None = None
             for proxy_attempt in range(1, max(1, proxy_attempts if proxies else 1) + 1):
@@ -681,9 +680,6 @@ def process_one_account(
                 try:
                     if not candidate.init_anonymous_session():
                         raise requests.RequestException("匿名会话初始化失败")
-                    if turnstile_solver:
-                        log(thread_id, "🧩 获取浏览器验证令牌...")
-                        captcha_token = turnstile_solver(candidate.proxy_url)
                     todo = candidate
                     break
                 except Exception as error:
@@ -713,11 +709,45 @@ def process_one_account(

             # ── 2. 发送验证码 ──
             log(thread_id, "📤 发送 OTP...")
-            otp_sent = todo.send_otp(email, captcha_token)
+            otp_sent = todo.send_otp(email)
             if not otp_sent and getattr(todo, "last_otp_error_code", "") == "CAPTCHA_REQUIRED" and turnstile_solver:
-                log(thread_id, "🧩 浏览器令牌已过期，重新获取...")
-                captcha_token = turnstile_solver(todo.proxy_url)
-                otp_sent = todo.send_otp(email, captcha_token)
+                challenge_error: Exception | None = None
+                challenge_attempts = max(1, proxy_attempts if proxies else 1)
+                for challenge_attempt in range(1, challenge_attempts + 1):
+                    candidate = todo if challenge_attempt == 1 else TodoforAI(
+                        proxies=resolve_proxy_templates(proxies)
+                    )
+                    try:
+                        if challenge_attempt > 1 and not candidate.init_anonymous_session():
+                            raise requests.RequestException("匿名会话初始化失败")
+                        log(thread_id, "🧩 目标要求浏览器验证，获取令牌后重试...")
+                        captcha_token = turnstile_solver(candidate.proxy_url)
+                        otp_sent = candidate.send_otp(email, captcha_token)
+                        if (
+                            not otp_sent
+                            and getattr(candidate, "last_otp_error_code", "")
+                            == "CAPTCHA_REQUIRED"
+                        ):
+                            raise RuntimeError(
+                                getattr(candidate, "last_otp_error_message", "")
+                                or "浏览器令牌未通过"
+                            )
+                        todo = candidate
+                        challenge_error = None
+                        break
+                    except Exception as error:
+                        challenge_error = error
+                        session = getattr(candidate, "session", None)
+                        if session is not None:
+                            session.close()
+                        if challenge_attempt < challenge_attempts:
+                            log(
+                                thread_id,
+                                "   当前代理或浏览器不可用，换 UUID... "
+                                f"({challenge_attempt}/{challenge_attempts})",
+                            )
+                if challenge_error is not None:
+                    raise challenge_error

             if not otp_sent:
                 error_code = str(getattr(todo, "last_otp_error_code", "") or "UNKNOWN")
